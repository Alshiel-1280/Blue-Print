# v0.1 検証記録

検証日: 2026-07-21

## 自動検証

- `swift test --scratch-path /tmp/blueprint-build --parallel`: 17件成功
- `swift build -c release --scratch-path /tmp/blueprint-release-build`: 成功
- `swift format lint --recursive --strict Sources Tests Package.swift`: 成功
- `swift package describe`: macOS 14、4ライブラリ、1実行ターゲット、4テストターゲットを確認
- `plutil -lint Resources/Info.plist`: 成功
- `sh -n scripts/package-app.sh`: 成功
- `git diff --check`: 成功

## 永続化・移行

- 初回セットアップを単一トランザクションで保存し、失敗時の部分反映がないことをテストした。
- DBを閉じて再度開いた後も事業者、年度、標準勘定科目が復元されることをテストした。
- v1サンプルDBをv2へ移行し、事前バックアップと既存データ保持をテストした。
- 監査イベントの更新・削除をDBトリガーで拒否することをテストした。
- 勘定科目の物理削除を拒否し、無効化と監査イベントをテストした。

## GUI受入検証

- 初回セットアップの必須入力、標準科目15件の生成、科目無効化、監査記録、再起動後の保持を実機操作した。
- キーボードショートカットとVoiceOver向けラベル・ヘルプ・表構造を確認した。
- 視覚比較と修正履歴はリポジトリルートの `design-qa.md` に記録した。

## 既知の制限

- v0.1はデータ基盤とマスター設定まで。仕訳、帳簿集計、インポート、バックアップ復元は後続バージョンで実装する。
- Developer ID署名、公証、配布用DMGは後続の配布マイルストーンで実装する。
