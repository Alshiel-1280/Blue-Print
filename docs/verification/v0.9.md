# v0.9 検証記録

## 自動検証

- `swift format lint --strict`: 合格
- plist、JSON、シェル構文: 合格
- 全テスト: 104件、失敗0件
- release性能ベンチマーク: 7基準すべて合格
- release build: 合格

## 実アプリQA

- `⌘2`で取引入力へ移動し、摘要へ初期フォーカスすることを確認
- Tabで摘要から金額へ進み、主要入力のVoiceOver名を確認
- `⇧⌘0`でデータ管理へ移動し、サイドバーが「メインナビゲーション」と読み上げられることを確認
- 全データ出力がバックグラウンドで完了し、ファイル書き出しシートを表示することを確認
- 短いパスフレーズで「原因・影響・修正方法」のアラートを確認
- ダークモードで案2参照と同一キャプチャ幅に並べ、3列カード、上部切替、余白、状態文言を再比較

## 配布

- 自己ビルドは`self-built release`、公式コンパイル条件は`official`を表示
- Developer ID署名、hardened runtime、Notary Service、staple、SHA-256生成を`release-app.sh`へ実装
- 証明書・Notary資格情報がないため、公式署名・公証の実行だけを未完了の公開ゲートとして残した

## 外部環境待ち

- 2025年分e-Tax WEB版への`.xtx`実地読込
- Developer ID署名・Apple公証済み公式バイナリの作成

この2点は機能合格へ読み替えず、v1公開前の手動ゲートとしてリリース候補に明記した。
